fantasize_about_character_effect = { 
		if = {
			limit = {
				Not = { has_character_flag = fantasizes_about_family }
				relation_with_character_is_incestuous_in_my_faith_trigger = { CHARACTER = $CHARACTER$ }
				Not = { scope:root_fantasize_character = $CHARACTER$ }
			}
			add_character_flag = fantasizes_about_family
		}
		if = { 
			limit = { 
				$CHARACTER$ = { has_variable = visual_age }
				has_variable = aging_attraction_offset
			}
			change_variable = {
				name = aging_attraction_offset
				add = {
					value = $CHARACTER$.var:visual_age
					subtract = var:aging_attraction_offset
					max = 0.04
					min = -0.04
				}
			}
		}
		if = { 
			limit = { 
				$CHARACTER$ = { is_male = yes }
				has_variable = penis_size_preference
			}
			change_variable = {
				name = penis_size_preference
				add = {
					value = $CHARACTER$.actual_penis_size
					subtract = var:penis_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_butt_size_preference
				add = {
					value = $CHARACTER$.actual_butt_size
					subtract = var:male_butt_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_muscle_preference
				add = {
					value = $CHARACTER$.actual_muscle
					subtract = var:male_muscle_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_fat_preference
				add = {
					value = $CHARACTER$.actual_fat
					subtract = var:male_fat_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_height_preference
				add = {
					value = $CHARACTER$.actual_height
					subtract = var:male_height_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
		}
		else_if = { 
			limit = { 
				$CHARACTER$ = { is_female = yes }
				has_variable = breast_size_preference
			}
			change_variable = {
				name = breast_size_preference
				add = {
					value = $CHARACTER$.actual_breast_size
					subtract = var:breast_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_butt_size_preference
				add = {
					value = $CHARACTER$.actual_butt_size
					subtract = var:female_butt_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_muscle_preference
				add = {
					value = $CHARACTER$.actual_muscle
					subtract = var:female_muscle_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_fat_preference
				add = {
					value = $CHARACTER$.actual_fat
					subtract = var:female_fat_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_height_preference
				add = {
					value = $CHARACTER$.actual_height
					subtract = var:female_height_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
		}
		if = { 
			limit = { 
				is_male = yes
				has_variable = penis_size_preference
				$CHARACTER$ = { has_variable = penis_size_preference }
			}
			change_variable = {
				name = penis_size_preference
				add = {
					value = $CHARACTER$.var:penis_size_preference
					subtract = var:penis_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_butt_size_preference
				add = {
					value = $CHARACTER$.var:male_butt_size_preference
					subtract = var:male_butt_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_muscle_preference
				add = {
					value = $CHARACTER$.var:male_muscle_preference
					subtract = var:male_muscle_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_fat_preference
				add = {
					value = $CHARACTER$.var:male_fat_preference
					subtract = var:male_fat_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = male_height_preference
				add = {
					value = $CHARACTER$.var:male_height_preference
					subtract = var:male_height_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
		}
		else_if = { 
			limit = { 
				is_female = yes
				has_variable = breast_size_preference
				$CHARACTER$ = { has_variable = breast_size_preference }
			}
			change_variable = {
				name = breast_size_preference
				add = {
					value = $CHARACTER$.var:breast_size_preference
					subtract = var:breast_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_butt_size_preference
				add = {
					value = $CHARACTER$.var:female_butt_size_preference
					subtract = var:female_butt_size_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_muscle_preference
				add = {
					value = $CHARACTER$.var:female_muscle_preference
					subtract = var:female_muscle_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_fat_preference
				add = {
					value = $CHARACTER$.var:female_fat_preference
					subtract = var:female_fat_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
			change_variable = {
				name = female_height_preference
				add = {
					value = $CHARACTER$.var:female_height_preference
					subtract = var:female_height_preference
					max = max_yearly_preference_increase
					min = max_yearly_preference_decrease
				}
			}
		}
	}