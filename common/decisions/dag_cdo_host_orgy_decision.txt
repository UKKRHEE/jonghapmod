dag_cdo_host_orgy_decision = {
	picture = "gfx/interface/illustrations/decisions/decision_misc.dds"
	ai_check_interval = 60

	cost = { gold = medium_feast_cost }

	#cooldown = { days = standard_feast_cooldown_time }

	desc = dag_cdo_host_orgy_decision_desc
	selection_tooltip = dag_cdo_host_orgy_decision_tooltip

	is_shown = {
		is_landed = yes
		highest_held_title_tier > tier_barony
	}

	is_valid_showing_failures_only = {
		is_available_adult = yes
	}

	effect = {
		capital_province = {
			spawn_activity = {
				type = dag_cdo_activity_orgy
#				type = activity_orgy
				owner = root
			}
		}
	}
	
	ai_potential = {
		short_term_gold > medium_feast_cost
	}

	ai_will_do = {
		base = -40				#~This value is the negative sum of the stress and gold modifiers.
		ai_value_modifier = {
			#ai_sociability#	#~This is mainly based on Gregarious, which explains all the odd requests
			ai_boldness = 0.2
			ai_energy = 0.1				
			ai_greed = -0.5
			ai_honor = -0.5
		}
		modifier = {			#~Will not call if they have been to a recent orgy. This is to reduce the orgy spam that happens.
			add = -1000			#~NB: We do not want this to actually stop an ai actually attending an orgy, just not hosting one.
			has_character_flag = dag_cdo_attended_recent_orgy_flag
		}
		modifier = {
			add = 25
			stress > low_stress
		}
		modifier = {
			add = 15
			short_term_gold > 450
		}
		dag_cdo_orgy_ai_will_do_modifier = yes						#runs from -120 to +120
	}
}

