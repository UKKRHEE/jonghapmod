dag_cdo_action_can_slave_train = {
	combine_into_one = yes

	check_create_action = {
		every_relation = {
			type = slave
			limit = {
				dag_cdo_slave_can_be_slave_trained_trigger = { TRAINER = root }
				NOT = { any_targeting_scheme = { scheme_owner = root } }
			}
			try_create_important_action = {
				important_action_type = dag_cdo_action_can_slave_train
				actor = root
				recipient = this
			}
		}
	}
	effect = {
		open_interaction_window = {
			interaction = dag_cdo_slave_training_interaction 
			actor = scope:actor
			recipient = scope:recipient
		}
	}
}