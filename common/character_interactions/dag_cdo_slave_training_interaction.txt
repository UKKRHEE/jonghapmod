dag_cdo_slave_training_interaction  = {
	category = interaction_category_prison

	common_interaction = yes
	interface_priority = 150

	auto_accept = yes
	ignores_pending_interaction_block = yes

	scheme = dag_cdo_slave_training

	send_name = DAG_CDO_START_SLAVE_TRAINING

	desc = dag_cdo_slave_training_interaction_desc

	is_shown = {
		scope:recipient = {
			dag_cdo_slave_can_be_slave_trained_trigger = { TRAINER = scope:actor }
		}
	}

	is_valid = {
		scope:actor = { is_ai = no }
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			can_start_scheme = {
				type = dag_cdo_slave_training		#~this was set to sway and I am not sure why
#				type = sway
				target = scope:recipient
			}
		}
	}

	on_accept = {
		scope:actor = {
			hidden_effect = {
				send_interface_toast = {
					title = dag_cdo_slave_training_interaction_toast
					left_icon = scope:actor
					right_icon = scope:recipient

					start_scheme = {
						target = scope:recipient
						type = dag_cdo_slave_training
						#type = test_scheme
					}
				}
			}
		}
	}
}


