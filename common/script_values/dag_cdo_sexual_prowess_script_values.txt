#~what would be nice is a seventh statistic, but fuck knows how you would do that.
#~It would be more suitable at the framework level anyway.

#~This probably should have some effect of the arousal frameworks sex evaluation stuff, when/if we look to integrate with that

dag_cdo_sexual_prowess_value = {
	value = 0
	#~Base
	add = {
		value = diplomacy
		multiply = 0.3
	}
	add = {
		value = martial
		multiply = 0.3
	}
	add = {
		value = intrigue
		multiply = 0.3
	}

	#~Lifestyle traits
	if = {
		limit = { has_trait = seducer }
		add = 1
	}
	if = {
		limit = { has_trait = prostitute_1 }
		add = 1
	}
	if = {
		limit = { has_trait = prostitute_2 }
		add = 2
	}
	if = {
		limit = { has_trait = prostitute_3 }
		add = 3
	}
	# criminal traits
	if = {
		limit = { has_trait = deviant }
		add = 1
	}	
	# stress traits
	if = {
		limit = { has_trait = rakish }
		add = 2
	}
	if = {
		limit = { has_trait = celibate }
		multiply = 0
	}

	#~Age
	#~well this fucks up everything
#	if = {
#		limit = { 
#			NOT = { 
#				has_character_modifier = no_prowess_loss_from_age 
#			}
#		}
#	}

	round = yes
	min = 0
	max = 100
}