targetted_decisions = {
	dwe_prisoner_offer_body = {
		filter = court
		ai_target_filter = court
		ai_check_interval = 12
		from_potential = {
			has_rape_content = yes
			is_female = yes
			practical_age = 16
			practical_age < 45
			prisoner = yes
			host = { character = ROOT }
			NOT = {
				has_character_flag = dwe_made_sexual_offer
			}
		}
		potential = {
			NOR = {
				trait = eunuch
				trait = celibate
			}
			prisoner = no
			same_realm = FROM
			practical_age = 16
			OR = {
				is_female = no
				AND = {
					is_female = yes
					trait = homosexual
				}
			}
			is_ruler = yes
		}
		allow = {
			always = yes
		}
		effect = {
			FROM = {
				set_character_flag = dwe_made_sexual_offer
				narrative_event = { id = dwe_prisoner.1 }
			}
		}
		ai_will_do = {
    		factor = 0.5
			modifier = {
				factor = 0.2
				FROM = {
					OR = {
						trait = chaste
						trait = virgin
					}
				}
			}
			modifier = {
				factor = 2
				FROM = {
					OR = {
						trait = lustful
						trait = hedonist
						trait = seductress
					}
				}
			}
			modifier = {
				factor = 0
					FROM = {
					OR = {
						trait = celibate
						trait = nun
					}
				}
			}
		}
	}
}
