### This file contains events specific to the Orc portion of the Dark World mod

namespace = DWOrc

# .10 The Horde Cometh
narrative_event = {
	id = DWOrc.10
	picture = a_placeholder # should be overwritten by the desc multipath
	is_triggered_only = yes # Triggered by Decision
	title = dw_orc_start_title	
	trigger = {
		has_fae_content = yes # Content check
	}
	desc = {
		trigger = {
			is_female = no # Male
		}
		text = DWOrc10m
		picture = orc_start2
	}
	desc = {
		trigger = {
			is_female = yes # Female
		}
		text = DWOrc10f
		picture = orc_start1
	}
	option = { # For the Horde!
		name = DWOrc10A
		add_trait = orc
		culture = orcish
		capital_scope = {
			culture = orcish
		}		
		hidden_tooltip = {
			add_random_orc_portrait = yes
		}
	}
	option = { 
		name = DWFOrc2
		add_trait = orc
		culture = orcish
		capital_scope = {
			culture = orcish
		}
		hidden_tooltip = {
			add_random_orc_portrait = yes
		}
		custom_tooltip = {
			text = DWAllRealmCharacters
		}
		hidden_tooltip = {
			character_event = { id = DWOrc.11 }
		}
	}	
}

character_event = {
	id = DWOrc.11
	hide_window = yes
	is_triggered_only = yes # Triggered by Decision
	immediate = {
		any_realm_character = {
			limit = {
				any_liege = { character = ROOT }
			}
			character_event = { id = DWSeeding.4 }
		}
	}
}