pregnancy = {
	usage = game
	selection_behavior = max
	pregnant = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant
				value = {
					value = 1
					if = {
						# Overweight females should have less visible pregnancy, so we change the 100% pregnant value
						limit = { current_weight > overweight_threshold }
						value = current_weight_for_portrait # An absolute and normalized value derived from character body weight
						multiply = -1.0 
						add = 1.5
						max = 1.0
					}
					multiply = {
						add = pregnancy_days
						divide = 250 # PREGNANCY_DAYS is 280; we want 1 month at 100%
						max = 1
					}
				}
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 100
				has_trait = pregnant
			}
		}
	}
	pregnant_twins = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant_twins
				value = {
					value = 1
					if = {
						# Overweight females should have less visible pregnancy, so we change the 100% pregnant value
						limit = { current_weight > overweight_threshold }
						value = current_weight_for_portrait # An absolute and normalized value derived from character body weight
						multiply = -1.0 
						add = 1.5
						max = 1.0
					}
					multiply = {
						add = pregnancy_days
						divide = 250 # PREGNANCY_DAYS is 280; we want 1 month at 100%
						max = 1
					}
				}
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 200
				has_trait = pregnant
				has_character_flag = twins
			}
		}
	}
	pregnant_triplets = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant_triplets
				value = {
					value = 1
					if = {
						# Overweight females should have less visible pregnancy, so we change the 100% pregnant value
						limit = { current_weight > overweight_threshold }
						value = current_weight_for_portrait # An absolute and normalized value derived from character body weight
						multiply = -1.0 
						add = 1.5
						max = 1.0
					}
					multiply = {
						add = pregnancy_days
						divide = 250 # PREGNANCY_DAYS is 280; we want 1 month at 100%
						max = 1
					}
				}
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 300
				has_trait = pregnant
				has_character_flag = triplets
			}
		}
	}
	pregnant_quadruplets = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant_quadruplets
				value = {
					value = 1
					if = {
						# Overweight females should have less visible pregnancy, so we change the 100% pregnant value
						limit = { current_weight > overweight_threshold }
						value = current_weight_for_portrait # An absolute and normalized value derived from character body weight
						multiply = -1.0 
						add = 1.5
						max = 1.0
					}
					multiply = {
						add = pregnancy_days
						divide = 250 # PREGNANCY_DAYS is 280; we want 1 month at 100%
						max = 1
					}
				}
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 400
				has_trait = pregnant
				has_character_flag = quadruplets
			}
		}
	}
	pregnant_quintuplets = {
		ignore_outfit_tags = yes
		dna_modifiers = {
			morph = {
				mode = add
				gene = gene_bs_pregnant
				template = pregnant_quintuplets
				value = {
					value = 1
					if = {
						# Overweight females should have less visible pregnancy, so we change the 100% pregnant value
						limit = { current_weight > overweight_threshold }
						value = current_weight_for_portrait # An absolute and normalized value derived from character body weight
						multiply = -1.0 
						add = 1.5
						max = 1.0
					}
					multiply = {
						add = pregnancy_days
						divide = 250 # PREGNANCY_DAYS is 280; we want 1 month at 100%
						max = 1
					}
				}
			}
		}
		weight = {
			base = 0
			modifier = {
				add = 500
				has_trait = pregnant
				has_character_flag = quintuplets
			}
		}
	}
}
